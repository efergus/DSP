
export let noiseProcessor = {
    url: new URL('./noise-processor.ts', import.meta.url),
    name: 'noise-processor',
}

export let audioTapInfo = {
    url: new URL('./audio-tap.ts', import.meta.url),
    name: 'audio-tap',
}